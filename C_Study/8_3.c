#include <stdio.h>

//int main() {
//	void swap(int *p1, int *p2);
//	int * p1, *p2, *p, a, b;
//	scanf_s("%d,%d", &a, &b);
//	p1 = &a;
//	p2 = &b;
//	if (a < b) {
//		swap(p1,p2);
//	}
//	printf("a=%d,b=%d", a, b);
//	printf("max=%d,min=%d", *p1, *p2);
//}
//
//void swap(int *p1, int *p2) {
//	int temp;
//	temp = *p1;//*p1表示a *p2表示b 所以这里ab的值交换了
//	*p1 = *p2;
//	*p2 = temp;
//}