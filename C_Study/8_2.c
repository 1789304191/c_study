#include <stdio.h>

//int main() {
//	int * p1, *p2, *p, a, b;
//	scanf_s("%d,%d", &a, &b);
//	p1 = &a;  //p1指向变量a的地址
//	p2 = &b;  //p2指向变量b的地址
//	if (a < b) {
//		p = p1; p1 = p2; p2 = p;  //把p1和p2的指向交换但是ab的值不变
//	}
//	printf("a=%d,b=%d", a, b);
//	printf("max=%d,min=%d", * p1, * p2);
//}